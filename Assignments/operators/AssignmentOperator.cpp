#include <iostream>

using namespace std;
int main()
{
    double a=5, b=6, c; //values for a and b are assigned
    a+=b;  //equivalent to a=a+b
    cout <<" enter the value for c :";
    cin >> c;
    a*=c;  // a=a*c
    cout<<a;

    return 0;
}
